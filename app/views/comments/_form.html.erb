<div class="comment">
  <%= form_with(model: [post, comment]) do |form| %>
    <%= render "shared/form_errors", model: comment %>

    <%= form.text_area :body, autocomplete: "off", placeholder: "Write a comment..." %>

    <%= form.submit %>
  <% end %>
</div>