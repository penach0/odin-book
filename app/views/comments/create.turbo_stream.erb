<%= turbo_stream.append nested_dom_id(@commentable, "comments"), @comment %>

<%= turbo_stream.update nested_dom_id(@commentable, "new_comment") do %>
    <%= render "form", comment: Comment.new, commentable: @commentable %>
<% end %>

<%= stream_counter_update model: @commentable, type: :comments %>
