<%= turbo_stream.append nested_dom_id(@post, "comments"), @comment %>

<%= turbo_stream.update nested_dom_id(@post, "new_comment") do %>
    <%= render "form", comment: Comment.new, post: @post %>
<% end %>