<!DOCTYPE html>
<html>
  <head>
    <title>OdinBook</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <nav>
      <h1><%= link_to "Odinbook", root_path %></h1>

      <% if user_signed_in? %>
        <p>Signed in as <%= current_user.profile.full_name %>. <%= link_to "Sign Out", destroy_user_session_path, data: { turbo_method: :delete,
                                                                                                              turbo_confirm: "Are you sure?"} %>
        </p>

        <ul>
          <% current_user.received_requests.each do |request| %>
            <li><%= request.email  %><%= render "request_response" %></li>
          <% end %>
        </ul>
      <% else %>
        <p><%= link_to "Sign In", new_user_session_path %> or <%= link_to "Sign Up", new_user_registration_path %></p>
      <% end %>

    </nav>

    <%= yield %>
  </body>
</html>
