<span>
    <%= button_to "Accept", friendship_path(friendship), method: :patch, params: { friendship: { status: "accepted" } } %>
    <%= button_to "Decline", friendship_path(friendship), method: :patch, params: { friendship: { status: "rejected" }} %> 
</span>