<h2>All Users</h2>

<ul>
    <% @users.each do |user| %>
        <li>
            <%= user.email %>
            <%= link_to "Send Request", friendships_create_path(friend_id: user.id) %>
        </li>
    <% end %>
</ul>
